ultralytics>=8.4.0
opencv-python>=4.9.0
torch>=2.0.0
torchvision
numpy
flask>=3.0.0
pyyaml

# --- CALM-VAD decision layer (calm/) ---
scikit-learn>=1.3      # M3 isotonic calibration, B1/B2 baselines
scipy>=1.10            # M4 exact Poisson / chi-square bound (has a pure-python fallback)
